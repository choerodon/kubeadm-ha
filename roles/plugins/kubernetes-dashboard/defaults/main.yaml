# 提权操作
ansible_become: true

# 是否等待启动
wait_plugins_ready: true

# 是否启用kubernetes-dashboard 
kubernetes_dashboard_enabled: true

# kubernetes-dashboard 镜像地址
kubernetes_dashboard_image: registry.aliyuncs.com/kubeadm-ha/kubernetes-dashboard-amd64:v1.10.1